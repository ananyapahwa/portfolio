# Ananya Pahwa — Portfolio

React + Vite rebuild of the static design reference.

```bash
npm install
npm run dev
```

```bash
npm run build
npm run preview
```

## Deploy on Vercel

Import the repo in Vercel. Framework is Vite (`vercel.json`); build output is `dist`.
